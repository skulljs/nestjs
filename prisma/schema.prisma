generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model cats {
  id         Int    @id @default(autoincrement())
  name       String
  color      String
  weight     String
  eyes_color String
}
